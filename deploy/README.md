# Deployment helper files

These are templates for Oracle Cloud:

- `horror-script-automation.service` → systemd service
- `nginx.conf` → reverse proxy on port 80

Replace the paths only if your repository lives somewhere other than `/home/ubuntu/horror-script-automation`.
